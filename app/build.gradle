plugins {
    id 'com.android.application'
}

android {
    namespace 'no.probenk.widget'
    compileSdk 35

    defaultConfig {
        applicationId 'no.probenk.widget'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
